@page "/Todo"
@using hub.Client.ViewModels.Todo
@inject ITodoViewModel TodoViewModel

@{
	var tabs = new (string, RenderFragment)[]
	{
		("Today", null),
		("All", @<TodoAllView />),
	};
}

<TabbedContent Tabs="@tabs" />